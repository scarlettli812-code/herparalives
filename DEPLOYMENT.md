# 环境与上线清单

| 环境 | 入口 | 数据 | AI | 用途 |
|---|---|---|---|---|
| development | 本地 | 浏览器 / 模拟 | 安全模板 | 开发 |
| staging | 预览分支 | CloudBase 测试环境 | 百炼真实调用 | 内容与压力验收 |
| demo | 临时域名 | 独立活动环境 | 预设零调用，自定义可降级 | Demo Day |
| production | 备案自定义域名 | CloudBase 生产环境 | 百炼真实调用 | 正式上线 |

## Demo Day

- 冻结并记录 `/api/presets` 的版本号。
- 断开模型密钥，完整玩通指定预设角色。
- 以 50 个并发会话验证首页、角色大厅和预设剧情。
- 自定义文本并发上限 20、图片并发上限 5；超过时显示队列状态。
- 准备完整录屏；模型不可用时隐藏自定义入口。

## 正式切换

- CloudBase 必须选择上海地域，以启用手机号验证码登录。
- 数据库、对象存储、认证环境与测试环境隔离。
- EdgeOne 中只配置服务端密钥；浏览器可见环境变量不得包含凭证。
- 自定义域名完成 ICP 备案后再绑定生产环境。

## AI 故事生成（DashScope）

- 在阿里云百炼控制台开通 `qwen-plus`（或 `qwen3.7-max`）模型并创建 API key；key 只配置在 Vercel dashboard / 本地 `.env.local`，绝不提交仓库。
- 生成链路：`/create`（角色卡，plus）→ `/prepare`（5 章大纲 + 第一章，plus）→ 阅读过程中按章生成（`/api/chapters/generate`，plus）。
- 默认模型 `qwen-plus`：约 90 秒/章，稳定在超时窗口内；想提升文本质感可设 `QWEN_STORY_MODEL=qwen3.7-max`，但单章常需 110–170 秒且超时仍计费。
- 下一章只会在章末选择已经记录后后台预生成；缓存绑定 `stateVersion`，回溯或换选项后不会复用旧分支。失败时回退到点击时按需生成。
- Vercel Hobby 计划函数最长 10 秒，Pro 默认 60 秒（可调至 300 秒）；qwen 生成整章可能需要 30–90 秒，三个生成接口均已声明 `maxDuration = 60/300`，需 Pro 计划或自托管才能生效。
- 无 key 或调用失败时自动回退：角色卡 → 规则分类器，故事 → 内置安全模板，无需额外处理。

## AI 章节插图（Wan 2.7）

- 自定义故事每章生成一张关键场景图，输入为用户所选立绘的 Base64 与当前章节实时正文；预设故事仍使用审核过的内置插图。
- 配置 `WAN_IMAGE_MODEL=wan2.7-image-pro`；建议将 `DASHSCOPE_IMAGE_ENDPOINT` 设为百炼业务空间对应地域的同步 multimodal endpoint。
- Wan 临时 URL 约 24 小时有效。当前游客会话同样 24 小时；若未来支持长期账号存档，必须增加对象存储转存任务。
- 图片接口失败不会阻断故事，界面改为所选立绘并显示失败状态。
